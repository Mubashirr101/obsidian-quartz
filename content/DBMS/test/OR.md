# Syntax

```sql
SELECT * FROM person WHERE (country_of_birth = 'China' OR country_of_birth = 'Ireland');
```

# Description

The ‘OR’ keyword is used to filter if either of any condition meets the criteria.

