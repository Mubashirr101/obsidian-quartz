# Syntax

```sql
# deletes the entire table
DELETE FROM person;

# deletes the entire row
DELETE email FROM person;

# deletes a value of a column of a specified row
DELETE email FROM person WHERE id = 126;
```

# Description

Can be used to delete a table, column or a column’s entry.
