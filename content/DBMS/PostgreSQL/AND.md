# Syntax

```sql
SELECT * FROM person WHERE gender = 'Male' AND country_of_birth = 'Afghanistan';
```

# Description

The ‘AND’ keyword is used to enter more than one filtering condition within the ‘WHERE’ clause.
