# Syntax

```sql
SELECT * FROM person WHERE country_of_birth IN ('China','Brazil','France');
```
# Description

The ‘IN’ keyword supports adding more than one value of a column in the filter as an array.
